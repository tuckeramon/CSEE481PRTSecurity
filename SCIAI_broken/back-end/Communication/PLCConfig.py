# Real PLC IP address (proxy forwards whitelisted traffic here)
PRT_PLC_REAL_IP = '192.168.1.51'

# Address that PLC.py connects to (routed through proxy when firewall is enabled)
PRT_PLC_IP_ADDRESS = '127.0.0.1'

# Set to False to bypass the proxy and connect directly to the PLC
PLC_FIREWALL_ENABLED = True

