# PLC IP address - backend connects directly (trusted local application)
PRT_PLC_IP_ADDRESS = '192.168.1.51'

# Enable the proxy firewall (listens on alternate ports for external hosts)
PLC_FIREWALL_ENABLED = True

